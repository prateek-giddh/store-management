import { createAction } from '@ngrx/store';

export const INCREMENT_ACTION = createAction('[Counter Component] Increment');
export const DECREMENT_ACTION = createAction('[Counter Component] Decrement');
export const RESET_ACTION = createAction('[Counter Component] Reset');
